import React from 'react'

const PortionHadding = ({text,className}) => {
  return (
    <h4 className={` font-open font-bold text-4xl text-secound ${className}`}>{text}</h4>
  )
}

export default PortionHadding